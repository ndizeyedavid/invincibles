export interface CreateConverstion {
  conversation_id: string;
  host_id: string;
  renter_id: string;
  property_id: string;
}

export interface UpdateConverstion {
  conversation_id: string;
  host_id: string;
  renter_id: string;
  property_id: string;
}
